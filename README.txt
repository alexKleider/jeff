
Proprietary information placed into an encrypted file
=====================================================
<recipes.tar.gz.gpg> can be uncrypted (use ~/Encrypted/encrypt.sh)
and then "tar -vxzf"ed to produce a "Recipes" directory.
